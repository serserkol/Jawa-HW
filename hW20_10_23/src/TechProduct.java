// TechProduct - это подкласс Product. Он наследует все свойства и методы класса Product.
class TechProduct extends Product {
    // Конструктор класса TechProduct.
    TechProduct(String name) {
        super(name); // Вызов конструктора суперкласса (Product) для инициализации имени.
    }
}
